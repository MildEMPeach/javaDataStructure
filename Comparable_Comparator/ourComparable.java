package Comparable_Comparator;

public interface ourComparable {
    public int compareTo(Object o);
}

// Actually, java provides official built-in interface called Comparable with the 
// same function.